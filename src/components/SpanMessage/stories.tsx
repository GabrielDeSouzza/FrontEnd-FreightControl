import { Meta, StoryObj } from '@storybook/react/';
import SpanMessage from '.';

export default {
  title: 'SpanMessage',
  component: SpanMessage,
} as Meta;

type Story = StoryObj<typeof SpanMessage>;
